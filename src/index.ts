/**
 * @packageDocumentation NPM package documentation template
 * @module index
 * @version **1.0.0**
 *
 * Basic usage example
 *
 * ```typescript
 * import { sum } from '@motix/npm-package-template';
 * const result = sum(1, 2);
 * console.log(result) // Outputs: 3
 * ```
 */
export * from './mul'
export * from './sum'
export * from './div'
